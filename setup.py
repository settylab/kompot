#!/usr/bin/env python

"""
This setup.py file is a thin wrapper around the configuration in pyproject.toml.
PEP 621 and PEP 631 define the pyproject.toml as the canonical place for build configuration.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()